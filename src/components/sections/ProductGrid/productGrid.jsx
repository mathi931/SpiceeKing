import React, { Component } from 'react';
import tw, { styled } from 'twin.macro';

const ProductGrid = () => {
	return <Container>ProductGrid</Container>;
};

export default ProductGrid;

const Container = tw.div``;